records: <pre><%= @records.map(&:label).to_json %></pre>
<br/>
addresses: <pre><%= @addresses.map(&:label) %></pre>
<br/>
current: <pre><%= @current %></pre>
<br/>
keys: <pre><%= @keys.map(&:label).to_json %></pre>
<br/>
events:
<% @events.map(&:label).each do |l| %>
    <pre><%= l %></pre>
<% end %>
